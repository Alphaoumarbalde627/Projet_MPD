; Configuration PHP pour les requêtes web (local + Hostinger)
; Ce fichier est lu automatiquement par PHP pour les requêtes HTTP
; Il doit rester dans le dossier public/ (document root)

upload_max_filesize = 50M
post_max_size = 50M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
